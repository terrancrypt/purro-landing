---
import xIcon from "../assets/xIcon.svg";
import discordIcon from "../assets/discordIcon.svg";
import telegramIcon from "../assets/teleIcon.svg";
import Button from "./Button";
import { BookTextIcon } from "@lucide/astro";

const socials = [
  {
    icon: xIcon.src,
    url: "https://x.com/purro_xyz",
    isFileIcon: false,
  },
  {
    icon: discordIcon.src,
    url: "https://discord.gg/TxvDwvfMTv",
    isFileIcon: false,
  },
  {
    icon: telegramIcon.src,
    url: "https://t.me/purro_xyz",
    isFileIcon: false,
  },
  {
    url: "https://docs.purro.xyz",
    isFileIcon: true,
  },
];
---

<header
  class="absolute top-0 left-1/2 max-w-6xl transform -translate-x-1/2 w-full container mx-auto mt-6 px-2 z-50 header-animate"
>
  <div
    class="p-3 flex justify-between items-center bg-[#b1cac4]/30 rounded-full"
  >
    <a href="/">
      <img src="/Purro_Icon_Dark.png" alt="logo" class="h-10 rounded-full" />
    </a>
    <div class="flex items-center gap-2">
      <div class="items-center flex">
        {
          socials.map((social) => (
            <a
              href={social.url}
              target="_blank"
              class="hover:bg-white/10 p-2 rounded-full transition-all duration-300"
            >
              {social.isFileIcon ? (
                <BookTextIcon class="h-6 w-6 text-white" />
              ) : (
                <img src={social.icon} alt="social icon" class="h-6" />
              )}
            </a>
          ))
        }
      </div>

      <a href="/install">
        <Button client:load> Install </Button>
      </a>
    </div>
  </div>
</header>
